name: CI/CD Pipeline

on:
  push:
    branches:
      - staging
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to GHCR
        run: echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u wellfound --password-stdin

      - name: Build Docker Image
        run: docker build -t ghcr.io/wellfound/app:${{ github.ref_name }} ./backend

      - name: Push Image
        run: docker push ghcr.io/wellfound/app:${{ github.ref_name }}

  deploy-staging:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/staging'

    steps:
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to Staging via SSM
        run: |
          aws ssm send-command \
            --instance-ids ${{ secrets.INSTANCE_ID_STAGING }} \
            --document-name "AWS-RunShellScript" \
            --parameters commands='[
              "docker pull ghcr.io/wellfound/app:staging",
              "docker stop app || true",
              "docker rm app || true",
              "docker run -d -p 80:3000 --name app ghcr.io/wellfound/app:staging"
            ]'

  deploy-production:
    runs-on: ubuntu-latest
    needs: deploy-staging
    if: github.ref == 'refs/heads/main'
    environment:
      name: production   

    steps:
      - name: Configure AWS
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to Production via SSM
        run: |
          aws ssm send-command \
            --instance-ids ${{ secrets.INSTANCE_ID_PROD }} \
            --document-name "AWS-RunShellScript" \
            --parameters commands='[
              "docker pull ghcr.io/wellfound/app:main",
              "docker stop app || true",
              "docker rm app || true",
              "docker run -d -p 80:3000 --name app ghcr.io/wellfound/app:main"
            ]'
